fastapi==0.135.3
uvicorn[standard]==0.44.0
httpx==0.28.1
python-dotenv==1.2.2

# Vector storage for the embedding pipeline. Embeddings themselves come from
# a local Ollama server over HTTP, so no SDK is needed.
sqlite-vec==0.1.9

# Tests
pytest-asyncio==1.2.0
